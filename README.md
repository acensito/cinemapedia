# cinemapedia

# RAMA DEV
## Instalacion
1. Clonar el repositorio
2. Ejecutar `flutter pub get` para instalar las dependencias
3. Renombrar el archivo `.env.template` a `.env` de la raiz del proyecto y colocar tu API Key de The Movie DB.
   ```
   THEMOVIEDB_API_KEY=tu_api_key
   ```

